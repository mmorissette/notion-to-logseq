RSpec.describe Notion::To::Logseq do
  it "has a version number" do
    expect(Notion::To::Logseq::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
